<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="movie"
            type="com.androidTestMid.movies.network.model.Movies.Movie" />

        <variable
            name="movieEventHandler"
            type="com.androidTestMid.movies.ui.MovieEventHandler" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        app:movie_event_handler="@{movieEventHandler}"
        app:movie="@{movie}"
        android:orientation="horizontal"
        android:background="@drawable/border_shadow"

        >


        <ImageView
            android:adjustViewBounds="true"
            android:maxWidth="@dimen/image_width"
            android:layout_height="@dimen/image_hight"
            android:layout_width="wrap_content"
            android:paddingRight="15dp"
            bind:image_url="@{movie.images.get(0)}" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:text="@{movie.title}" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="right"
            android:paddingRight="5dp"
            android:text="@{String.valueOf(movie.year)}" />
    </LinearLayout>
</layout>