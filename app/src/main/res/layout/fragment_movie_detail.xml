<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="movie"
            type="com.androidTestMid.movies.network.model.Movies.Movie" />
    </data>

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:clickable="true"
        android:background="@android:color/white"
        android:paddingLeft="@dimen/pading_movie_detail"
        android:paddingRight="@dimen/pading_movie_detail"
        tools:context="com.androidTestMid.movies.ui.MovieDetailFragment">

        <com.androidTestMid.movies.ui.ViewPagerCustomDuration xmlns:android="http://schemas.android.com/apk/res/android"
            android:id="@+id/pager"
            android:layout_width="wrap_content"
            android:layout_height="350dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="@dimen/pading_movie_detail"
            android:textSize="15sp"
            tools:text="Title"
            android:text="@{movie.intro}" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="15sp"
            tools:text="Title"
            android:text="@{movie.text}" />
    </LinearLayout>
</ScrollView>

    </layout>